<% if @cart.line_items.count > 0 %>
  <div class="container px-6 py-10 mx-auto view-height">
    <div class="text-right">
      <%= link_to 'Keep Shopping', product_path(id: @cart.line_items.last.product_id ), class: 'ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
    </div>
    <section class="section p-2">
      <%= render(@cart.line_items) %>

      <div class="flex justify-between items-center p-2">
        <div class="column">
          <%= button_to 'Empty Cart', @cart, method: :delete, data: { confirm: "Are you sure? " }, class: "ml-2 rounded-lg py-3 px-5 bg-red-300 inline-block font-medium" %>
        </div>
        <div class="column total has-text-right">
          <h4 class="title is-4">
            <span class="f5 has-text-grey">Total:</span> <%= number_to_currency(@cart.total_amount) %>
          </h4>
        </div>
      </div>
    </section>
  </div>
<% else %>
  <div class="container px-6 py-10 mx-auto view-height">
  <h1>No items</h1>
<% end %>
